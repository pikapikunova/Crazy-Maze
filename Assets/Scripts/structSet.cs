using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class structSet
{
    public HashSet<int> numOfSet;
    public int count;

    public structSet(HashSet<int> Num, int Count)
    {
        numOfSet = Num;
        count = Count;
    }
}
